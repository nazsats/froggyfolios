# Froggy Whitelist

A Next.js app for managing a whitelist form with Twitter authentication and Supabase backend.

## Setup
1. Clone the repo: `git clone https://github.com/yourusername/froggy-whitelist.git`
2. Install dependencies: `npm install`
3. Create a `.env.local` file with the following: